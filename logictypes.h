#ifndef LOGICTYPES_H
#define LOGICTYPES_H

enum class InteractionMode {
    Standard,
    Linking,
    Excluding
};

enum class LogicFunction {
    AND,
    OR,
    NOT,
    XOR,
    NAND,
    NOR
    //CUSTOM
};

#endif // LOGICTYPES_H
